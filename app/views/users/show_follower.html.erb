<% @user.followers.each do |user| %>

<table>
  <thead>
    <tr>
      <th>name</th>
      <th>show</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= user.name %></td>
      <td><%= link_to 'show', user_path(user) %></td>
    </tr>
  </tbody>
</table>
<% end %>
