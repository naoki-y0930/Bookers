<h1>Books</h1>

<table>
  <thead>
    <tr>
      <th>title</th>
      <th>opinion</th>
    </tr>
  </thead>
<tbody>
<% @books.each do |book| %>
  <tr>
    <td>user後で</td>
    <td><%= book.title %></td>
    <td><%= book.body %></td>
  </tr>
<% end %>
</tbody>
</table>
